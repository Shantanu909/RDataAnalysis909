# hw programs
#p1 Create a user define function named sum which accept 2 arguments (of integer type) and return the sum of them.
sum(num1,num2){
  s=num1+num2
}

n1=readline()
nu1=as.integer(n1)
n2=readline()
nu2=as.integer(n2)
sum(nu1,nu2)

a=mtcars
